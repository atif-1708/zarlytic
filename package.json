{
  "name": "zarlytics",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@google/genai": "latest",
    "@supabase/supabase-js": "latest",
    "lucide-react": "latest",
    "react": "latest",
    "react-dom": "latest",
    "recharts": "latest"
  },
  "scripts": {
    "build": "mkdir -p public && cp index.html metadata.json vercel.json public/ && cp *.tsx public/ 2>/dev/null || true && cp *.ts public/ 2>/dev/null || true && cp -r components public/ 2>/dev/null || true && cp -r features public/ 2>/dev/null || true",
    "start": "npx serve public"
  }
}
