{
  "name": "zarlytics",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@google/genai": "latest",
    "@supabase/supabase-js": "latest",
    "lucide-react": "latest",
    "react": "latest",
    "react-dom": "latest",
    "recharts": "latest"
  },
  "scripts": {
    "build": "echo 'Deploying Static ESM Project' && mkdir -p dist && cp index.html dist/ && cp *.tsx dist/ && cp *.ts dist/ && cp vercel.json dist/ && cp -r components dist/ || true && cp -r features dist/ || true",
    "start": "npx serve ."
  }
}
